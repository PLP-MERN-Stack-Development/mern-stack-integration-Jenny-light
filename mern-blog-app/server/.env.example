# ===== Example Environment Configuration for MERN Blog (Server) =====

# The port number for your Express server
PORT=5000

# MongoDB connection string
# Example for local MongoDB:
# MONGO_URI=mongodb://127.0.0.1:27017/blogdb
# Example for MongoDB Atlas:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/blogdb?retryWrites=true&w=majority
MONGO_URI=

# Secret key for signing JSON Web Tokens (JWT)
# Generate a random string with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Environment type (development, production, etc.)
NODE_ENV=development

# Optional: Cloudinary credentials for image uploads
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
